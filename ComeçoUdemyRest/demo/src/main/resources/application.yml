cors:
  originPatterns: "http://localhost:8080,https://erudio.com.br,http://localhost:3000"
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot?useTimezone=true&serverTimezone=UTC-3
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
  springdoc:
    pathsToMatch: /person/**
    swagger-ui:
      use-root-path: true

